# CUDA wheels for torch are installed from the PyTorch index; install them with:
#   pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 \
#       --index-url https://download.pytorch.org/whl/cu124
# Detectron2 0.6 is installed from source against torch 2.6 (no public wheel
# exists for this combination); see README for the exact install command.

torch==2.6.0+cu124
torchvision==0.21.0+cu124
numpy==2.0.1
opencv-python==4.13.0.92
pycocotools==2.0.11
matplotlib==3.10.9
Pillow
PyYAML
tqdm==4.67.3
wandb>=0.16            # experiment tracking (optional; gated behind WANDB.ENABLED)
fvcore==0.1.5.post20221221
iopath==0.1.9
hydra-core==1.3.2
omegaconf==2.3.0
# Detectron2 from a specific commit:
# git+https://github.com/facebookresearch/detectron2.git@b599f139756bd3646a26a909caf86a1a159e53a7
